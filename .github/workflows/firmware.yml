name: firmware

on:
  push:
    paths: ["firmware/**", "tools/**", ".github/workflows/firmware.yml"]
  pull_request:
    paths: ["firmware/**", "tools/**", ".github/workflows/firmware.yml"]

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Host unit tests (filters, AHRS, flight logic, protocol)
        run: |
          g++ -std=c++17 -O1 -Wall -Wextra -Werror -I firmware/butterfly_fc \
              firmware/tests/test_core.cpp -o test_core
          ./test_core
      - name: protocol.h identical in both sketches
        run: diff firmware/butterfly_fc/protocol.h firmware/ground_station/protocol.h
      - name: Python tool syntax
        run: python3 -m py_compile tools/gyro_fft.py

  compile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: arduino/compile-sketches@v1
        with:
          platforms: |
            - name: esp32:esp32
              source-url: https://espressif.github.io/arduino-esp32/package_esp32_index.json
          fqbn: esp32:esp32:XIAO_ESP32S3
          sketch-paths: |
            - firmware/butterfly_fc
            - firmware/ground_station
          enable-warnings-report: true
